<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.gaolonglong.fragmenttabhost">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".DiverApplication">

        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="com.driver.fileProvider"
            android:grantUriPermissions="true"
            android:exported="false">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".activities.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!--登录界面-->
        <activity android:name=".activities.LoginActivity"
            android:theme="@style/AppTheme">

        </activity>

        <!--添加回程页面-->
        <activity android:name=".activities.AddReleaseActivity"
            android:theme="@style/AppTheme">

        </activity>

        <!--设置界面-->
        <activity android:name=".activities.SettingActivity"
            android:theme="@style/AppTheme">

        </activity>

        <activity android:name=".activities.DebugApiActivity"
            android:theme="@style/AppTheme">


        </activity>
        <activity android:name=".activities.BaseActivity"
            android:theme="@style/AppTheme">

        </activity>
        <!--认证主界面-->
        <activity android:name=".activities.RenzhengMainActivity"
            android:theme="@style/AppTheme">

        </activity>
        <!--车队司机认证-->
        <activity android:name=".activities.CarGroupRenzhengActivity"
            android:theme="@style/AppTheme">

        </activity>
        <!--车队司机认证2-->
        <activity android:name=".activities.CarGroupRenzheng2Activity"
            android:theme="@style/AppTheme">

        </activity>

    </application>

</manifest>
